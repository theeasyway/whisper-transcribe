# API Keys (only needed if using the respective model)
FIREWORKS_API_KEY="your_fireworks_api_key_here"
OPENAI_API_KEY="your_openai_api_key_here"

# Model Selection - Choose one: "fireworks", "openai", "local"
TRANSCRIPTION_MODEL="fireworks"

# Local model configuration (only needed if TRANSCRIPTION_MODEL="local")
# LOCAL_MODEL_PATH="models"
 DEFAULT_MODEL_SIZE="small.en"  # Options: tiny.en, base.en, small.en, medium.en, large-v3
# USE_GPU="false"  # Set to "true" to enable GPU acceleration (requires CUDA)

# Hotkey configuration (optional)
# RECORDING_HOTKEY="f9"